class Ship {
    constructor(x, y, z){
        this.x = x;
        this.y = y;
        this.z = z;
        this.delta = 0
        this.receiveInput = true

        // move the ship
        window.addEventListener('click', () => {
            if (this.receiveInput) {

            }
        })
    }

}